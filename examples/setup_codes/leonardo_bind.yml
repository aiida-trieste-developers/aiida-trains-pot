## Setup for Leonardo Booster at Cineca.
# OK just for QE.

---
label: "leo1_scratch_bind"
hostname: "login01-ext.leonardo.cineca.it"
transport: "core.ssh"
scheduler: "core.slurm"
work_dir: "/leonardo_scratch/large/userexternal/dbidoggi/runs/aiida"
mpirun_command: "mpirun -np {tot_num_mpiprocs} --map-by socket:PE=8 --rank-by core"
mpiprocs_per_machine: "1"
prepend_text: |
  export OMP_NUM_THREADS=8
  export OMP_PLACES=cores
  export OMP_PROC_BIND=close

##  verdi computer setup --config leonardo_bind.yml
